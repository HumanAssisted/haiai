[package]
name = "hai-binding-core"
version = "0.4.1"
description = "Shared binding core for HAI SDK FFI (Python, Node, Go)"
readme = "README.md"
keywords = ["hai", "jacs", "agent", "ffi", "binding"]
categories = ["api-bindings"]
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
haiai = { version = "=0.4.1", path = "../haiai", features = ["conflict"] }
serde = { workspace = true }
serde_json.workspace = true
thiserror.workspace = true
tokio.workspace = true


[features]
# Agreements ride along by default so the FFI parity surface (fixture's
# `agreements` section) is complete in every shipped binding; opting out
# compiles cleanly because every agreement item is cfg-gated.
default = ["agreements", "conflict"]
agreements = ["haiai/agreements"]
conflict = ["haiai/conflict"]

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
regex = "1"
tempfile = "3"
# Used to generate test PNG bytes for media-signing wrapper tests (Layer 8).
image = { version = "0.25", default-features = false, features = ["png"] }
# Used to mock /api/v1/records for the doc-store wrapper tests (TASK_001).
# Version pinned to match `rust/haiai/Cargo.toml` so cargo resolves a single
# httpmock crate across the workspace.
httpmock = "0.8"
